version: '3.8'

services:

  postgres:
    image: postgres:15
    container_name: credits_postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: credits_db
    ports:
      - "5432:5432"